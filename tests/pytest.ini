[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    github: marks tests that clone from GitHub (deselect with -m "not github")
    integration: marks tests that need network or external services (deselect with -m "not integration")
